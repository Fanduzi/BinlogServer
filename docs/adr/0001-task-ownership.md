# 任务所有权收在一处

集群里「谁可以跑这个任务」曾经散落在调度器、执行器、App 装配和元数据查询里。决定：Worker 侧只留一处任务所有权——占上、续上、放开、认领（开机和平时同一件事）、Fail-safe Stop、封文件前再问一次。控制面只写 STARTING，不占租约。能不能跑以租约为准；任务行上的主人是抄本。FAILED 立刻放开租约；RETRY_BACKOFF 继续占着。单机也走同一扇门（进程内永远是自己），避免 `if 有租约` 两套路径。Worker 注册和心跳不是任务所有权。
