# 01: 过期租约和失败上传的查询必须写明白

**What to build:** Worker 去捡过期任务、去重试失败上传时，用的查询是说好的、注入进来的。少实现就报错或走不到「当没有」，不能再静默返回 0 条。

**Blocked by:** None (can start immediately)

**Status:** done

- [x] 认领过期租约在查询不可用时不会假装「没有过期任务」
- [x] 列出待重试上传在查询不可用时不会假装「没有失败文件」
- [x] 单机/测试 fake 显式提供这两项查询，或明确走内存等价实现
- [x] 现有认领、重试上传测试仍然通过
